services:
  chatbot:
    build: .

    container_name: company-faq-chatbot

    ports:
      - "8000:8000"

    environment:
      OLLAMA_URL: http://host.docker.internal:11434
      OLLAMA_MODEL: qwen2.5:1.5b

    restart: unless-stopped